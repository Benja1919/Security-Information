Inside server.py the function "handle_message" deals with posting a new message. We can see that when adding a new channel (using "join" there is not checking. so when channel is initialized with "#". after creating the channel - it's name will appear in the channel_list and afterwards (after channel's name) we added a script, so when the channel is added - the script will start running.
